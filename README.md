# motinder-api
API for our project 'Motinder'.

## Install the API
Clone the repository on any directory and inside run:
```console
npm init -y
npm i nodemon -D
```

## Run the API
```console
npm run dev
```
